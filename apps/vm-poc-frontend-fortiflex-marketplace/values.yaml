name: vm-poc-frontend-fortiflex-marketplace
image:
  tag: latest
replicas: 1
servicePort: 80
containerPort: 80

# NOTE: VITE_API_BASE_URL is a runtime env var (doesn't affect the built app)
# The frontend is built with VITE_BACKEND_HOST passed as a build arg in CI/CD
# For K8s deployment, it should be set to the backend's internal K8s service DNS
env: {}

ingress:
  enabled: true
  host: fortiflex-marketplace.fortinetcloudcse.com
  certificateArn: arn:aws:acm:us-east-1:228122752878:certificate/194f3eec-f018-47aa-be72-fec0868d16f0
